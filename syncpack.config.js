/** @type {import('syncpack').RcFile} */
const config = {
	semverGroups: [
		{
			dependencies: ["**"],
			dependencyTypes: ["**"],
			packages: ["**"],
			range: "",
		},
	],
};

export default config;
